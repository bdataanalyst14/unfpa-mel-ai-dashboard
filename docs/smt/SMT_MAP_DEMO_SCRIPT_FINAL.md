# SMT MAP DEMO SCRIPT FINAL

```text
--- SMT MAP DEMO SCRIPT (Caveat Mode) ---

1. Open the UNFPA MEL dashboard in the demo environment.
2. Navigate to **Geographic Coverage** (Dashboard > Geographic Coverage).
3. Verify that the map loads using the mock dataset (see `geographic-map-metrics.ts`).
4. Ensure the **caveat banner** is displayed at the top of the page:
   "This Geographic Coverage view is a protected‑map prototype using mock/aggregated data. Live connection is blocked."
5. Walk through the province and district labels, noting that they are readable but represent mock data.
6. Do NOT mention live data, GBV/OCMC, or small‑cell privacy details.
7. End the demo with a brief statement that the map is **SMT‑ready with caveats** and will be updated once freshness and suppression gates are cleared.
```

*No source files were edited; the demo script is documentation only.*
