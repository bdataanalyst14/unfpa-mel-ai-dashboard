# SMT MAP FINAL TALKING POINTS

- **Geographic Coverage view** is presented as a **protected‑map prototype** using mock/aggregated data.
- **Live geographic connection** is **blocked** until the freshness gate (E001) and suppression gate (E002) are cleared.
- The map **does not include** live GBV/OCMC data; those datasets remain disabled.
- Labels for provinces and districts are rendered from the mock dataset; they are **readable** but **not a live operational view**.
- A **caveat banner** is shown in the presenter script (see `SMT_MAP_CAVEAT_FINAL.md`).
- No changes have been made to any protected files (`nepal‑map‑base.ts`, `GeographicCoverageMap.tsx`, etc.).
- The demo is **SMT‑ready with caveats** – suitable for stakeholder illustration but not production use.
